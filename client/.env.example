# API base URL (use relative /api when same host, or full backend URL when deployed)
VITE_API_URL=/api

# When deploying frontend separately, set to your backend URL, e.g.:
# VITE_API_URL=https://your-backend.onrender.com/api
